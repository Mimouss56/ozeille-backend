model Budget {
  id    String  @id @default(uuid())
  label String  @db.VarChar(30)
  color String? @db.VarChar(7) // Pour stocker le code HEX de la couleur ex: #FFFFFF 
  userId String  @map("user_id")
  categories Category[]

  @@map("budgets")
}