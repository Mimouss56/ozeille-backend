model Frequency {
  id           String  @id @default(uuid())
  label        String  @db.VarChar()
  monthlyValue Decimal @db.Decimal(5,2) @unique
  transactions  Transaction[]

  @@map("frequencies")
}
