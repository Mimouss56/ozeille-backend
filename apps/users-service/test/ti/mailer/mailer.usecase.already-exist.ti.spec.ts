import { MailerAlreadyExistUsecase } from "src/mailer/usecase/mailer.usecase.already-exist";

describe("MailerAlreadyExistUsecase TI", () => {
  it("devrait être défini", () => {
    expect(MailerAlreadyExistUsecase).toBeDefined();
  });
});
