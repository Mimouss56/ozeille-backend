import { MailerUsecaseSendMail } from "src/mailer/usecase/mailer.usecase.send-mail";

describe("MailerUsecaseSendMail TI", () => {
  it("devrait être défini", () => {
    expect(MailerUsecaseSendMail).toBeDefined();
  });
});
