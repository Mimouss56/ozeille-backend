import { MailerUsecaseConfirmEmail } from "src/mailer/usecase/mailer.usecase.confirm-email";

describe("MailerUsecaseConfirmEmail TI", () => {
  it("devrait être défini", () => {
    expect(MailerUsecaseConfirmEmail).toBeDefined();
  });
});
